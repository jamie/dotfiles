# .zshenv - Zsh environment file, loaded always

# This file in $HOME bootstraps $ZDOTDIR and then loads from there.
# Subshells (where $ZDOTDIR is already defined) will skip this bootstrap file.

export ZDOTDIR=$HOME/.config/zsh
source $ZDOTDIR/.zshenv
